# Manifesto AI — Single-File Manifestos

React app that loads all countries/candidates from **one JSON file** (`data/manifestos.json`), validates entries, and computes **personal policy impact**.

## Run
```bash
npm install
npm start
